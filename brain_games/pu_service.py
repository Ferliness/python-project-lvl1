"""Module contains service pure functions common to games."""


def is_even(number):
    """Сhecks if a number is even, return True/False.

    Args:
        number (int): The number to check

    Returns:
        return: bool
    """
    return number % 2 == 0
